/** cameraTransitions.js — Stage 1 skeleton
 * Scroll-driven camera movement added in Stage 3.
 */
export function transitionCamera(camera, targetPos, targetLook) {
  // TODO Stage 3: GSAP tween camera.position + lookAt
}
