/** scrollAnimations.js — Stage 1 skeleton
 * GSAP ScrollTrigger integration added in Stage 3.
 */
export function initScrollAnimations() {
  // TODO Stage 3: import gsap + ScrollTrigger, attach to section elements
}
