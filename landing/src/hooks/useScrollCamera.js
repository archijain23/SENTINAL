/** useScrollCamera — Stage 1 skeleton */
import { useRef } from 'react';
export function useScrollCamera() {
  const cameraRef = useRef(null);
  // TODO Stage 3: ScrollTrigger-driven camera position updates
  return { cameraRef };
}
